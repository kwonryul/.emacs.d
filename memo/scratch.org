cljs 같은 경우 테스트와 디버깅은 따로 진행하지 않는다.

repl 환경 자체를 통해 대부분 디버깅을 해결하고

테스트 같은 경우도 직접 앱을 가지고 놀면서 실행....

spec 같은 경우는

1. reitit 쪽에서 검증
2. cofx 쪽에서 검증

나머지는 검증하지 않는다.

reitit 의 검증은 형변환 때문이고

cofx 는 왜 검증하는거지?

... 좀 더 진행해보자....



이벤트들은 테스트를 진행하고

이펙트들은 테스트를 진행하지 않는다.

코이펙트는 malli schema 를 체크하고

reitit에서도 malli schema를 체크한다

이펙트들에 대해 테스트를 진행하려면

input spec

cofx spec
